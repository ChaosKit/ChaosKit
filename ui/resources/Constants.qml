pragma Singleton
import QtQuick 2.0

QtObject {
  readonly property int fontSize: 12

  readonly property color backgroundColor: "#fff"
  readonly property color mainColor: "#bdbdbd"
  readonly property color accentColor: "#4caf50"

  readonly property color highlightColor: "#40ffffff"
}
